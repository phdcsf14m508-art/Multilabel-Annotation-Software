# Multilabel-Annotation-Software
A .NET C# desktop app for annotating vehicle images with bounding boxes and multi-label data (type, manufacturer model, color, plate), exporting results to JSON for ML training.
